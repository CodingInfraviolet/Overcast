#pragma once
extern double PI;
extern double halfPI;

extern float PIf;
extern float halfPIf;

/**Converts an input from radians to degrees.*/
float toDegrees(float x);

/**converts an input from degrees to radians.*/
float toRadians(float x);